pipeline {
    agent any

    stages {
        stage('Checkout confirmation') {
            steps {
                sh 'echo "Jenkins pipeline started on dev branch"'
                sh 'ls -la'
            }
        }
    }
}