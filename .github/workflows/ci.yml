name: devops-notes-manager CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v3

      - name: Say hello from CI
        run: echo "Hello from GitHub Actions CI"

      - name: list repo files
        run: ls -la
